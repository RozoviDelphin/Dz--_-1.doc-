#include <stdio.h>
int GG(int X)
{
    int i,min=9999;
    for (;X!=0;)
    {
        i=X%10;
        X=X/10;
        if (min>i)
        {
            min=i;
        }
    }
    return min;
    
}
int main ()
{
    int X;
    scanf("%d",&X);
    printf("%d ",GG(X));
}
